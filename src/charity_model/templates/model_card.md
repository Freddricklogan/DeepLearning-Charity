# Model card — charity funding outcome classifier

Generated by `charity-model report --seed $seed`; every number below was measured in that run.

## Model details
- Task: binary classification of `IS_SUCCESSFUL` (funding used effectively) from application features.
- Architecture: multilayer perceptron, hidden layers $hidden (ReLU, batch normalisation, dropout $dropout), sigmoid output; $params parameters; Keras 3 on the JAX backend.
- Training: Adam (lr $lr), batch $batch, up to $max_epochs epochs with early stopping (patience $patience) on validation loss, best weights restored; stopped after $epochs epochs, best at epoch $best_epoch.
- Baseline: L2-regularised logistic regression on the same features.

## Data
- $source.
- $rows rows; identifiers EIN and NAME dropped; rare categories folded into Other ($binned); ASK_AMT log-transformed; one-hot encoding → $features features.
- Stratified 75/25 split (seed $seed); scaler fitted on the training rows only. Test rows: $test_rows; positive rate $base_rate.

## Evaluation (held-out test rows, threshold 0.5)
| Metric | Logistic regression | Network |
|---|---|---|
| Accuracy | $b_acc | $n_acc |
| Precision | $b_prec | $n_prec |
| Recall | $b_rec | $n_rec |
| F1 | $b_f1 | $n_f1 |
| ROC AUC | $b_auc | $n_auc |
| Majority-class accuracy | $majority | same |

## Intended use and limits
- A teaching example of an honest tabular-classification pipeline. Not a decision system: the features describe applications, not outcomes, and the label's provenance is the exercise's, not audited.
- The network's advantage over logistic regression is $gap accuracy points on this split; treat differences of that size as within run-to-run variation unless repeated across seeds.
- No fairness analysis is possible: the dataset carries no protected attributes, and none should be inferred from organisation names.

## Reproduce
`uv run charity-model report --out dist --seed $seed` regenerates this card and the report from the vendored data.
